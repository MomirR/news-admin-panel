{
  "name": "news-admin-panel",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@material-ui/core": "^4.5.1",
    "@material-ui/icons": "^4.5.1",
    "axios": "^0.21.1",
    "dotenv": "^8.2.0",
    "formik": "^1.5.8",
    "moment": "^2.24.0",
    "react": "^16.10.2",
    "react-dom": "^16.10.2",
    "react-moment": "^0.9.6",
    "react-paginate": "^6.3.0",
    "react-router-dom": "^5.1.2",
    "react-scripts": "3.2.0",
    "yup": "^0.27.0"
  },
  "scripts": {
    "start": "react-app-rewired start",
    "build": "react-app-rewired build",
    "test": "react-app-rewired test",
    "eject": "react-scripts eject",
    "clean": "rm -rf ./node_modules package-lock.json",
    "rebuild": "npm run clean && npm i",
    "clean:prod": "rm -rf ./build",
    "prod": "npm run rebuild && npm run clean:prod && npm run build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "react-app-rewire-aliases": "^0.2.0",
    "react-app-rewired": "^2.1.4"
  }
}
